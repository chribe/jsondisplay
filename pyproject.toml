[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "jsondisplay"
version = "0.1.0"
dependencies = [
    "numpy",
    "plotly"
]

[tool.setuptools]
py-modules = ["jsondisplay"]

[project.scripts]
jsondisplay = "jsondisplay:main"